// This shopping list program isn't compiling!
// Use your knowledge of generics to fix it.


fn main() {
    //Making use of Rust's ability to "ignore" `_`'s.
    let mut shopping_list: Vec<_> = Vec::new();
    shopping_list.push("milk");
}
